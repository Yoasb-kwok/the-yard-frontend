{
  "name": "the-yard-frontend",
  "private": true,
  "description": "Frontend app lives in studio/ — run dev/build from there or via this root.",
  "scripts": {
    "dev": "npm run dev --prefix studio",
    "build": "npm install --prefix studio && npm run build --prefix studio",
    "preview": "npm run preview --prefix studio",
    "lint": "npm run lint --prefix studio",
    "typecheck": "npm run typecheck --prefix studio"
  }
}
